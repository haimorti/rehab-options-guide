[build]
  command = "npm run build"
  publish = "dist"

# SPA routing: redirect all routes to index.html so React's router handles navigation
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
